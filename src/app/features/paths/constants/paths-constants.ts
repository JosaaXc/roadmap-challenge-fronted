// Course titles a Mis rutas card lists before summing up the rest
export const PREVIEW_COURSES = 3;

// Mirrors CreateCustomNodeDto on the backend
export const RESOURCE_TITLE_MAX_LENGTH = 100;

// The backend accepts a URL without its scheme, but as an href it would resolve against this site
export const RESOURCE_URL_PATTERN = /^https?:\/\/\S+$/i;
